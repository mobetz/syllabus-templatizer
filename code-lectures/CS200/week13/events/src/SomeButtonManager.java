import javafx.scene.control.Button;
import javafx.scene.control.Label;

public class SomeButtonManager {

/*
    public setupButton(Button myButton, Label l) {


        int x = 0;
        myButton.setOnAction((e) -> {
            x = x + 1;
            l.setText(x);
        });

    } //<- X gets deleted right here.... but the button could be clicked any time as long as it still lives!
    //The lifetime of the button is longer than the lifetime of "x", so this is disallowed!

 */
}
